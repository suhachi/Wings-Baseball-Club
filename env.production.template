# Wings Baseball Club Community PWA - Production Environment Variable Template
# 이 파일을 .env.production 파일로 복사하여 실제 값을 입력하십시오.
# ⚠️ 주의: 실제 .env.production 파일은 보안상 절대 Git에 커밋하지 마십시오.

# [FCM Web Push 설정]
# Firebase Console > 프로젝트 설정 > 클라우드 메시징 > 웹 푸시 인증서(Key pair)에서 확인 가능
VITE_FCM_VAPID_KEY=YOUR_VAPID_PUBLIC_KEY_HERE
